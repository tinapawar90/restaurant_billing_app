# Restaurant Billing App (PyQt Desktop)

A simple desktop GUI built with PyQt5. Uses the same `models.py` and `billing.py` for logic.
Run locally:
```bash
pip install -r requirements.txt
python app.py
```
